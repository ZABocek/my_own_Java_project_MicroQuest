package com.example.microquest.model;

/**
 * Quest category tags used for filtering on the quest-list page.
 * Each quest belongs to exactly one category.
 */
public enum Category {
    FOOD,
    FITNESS,
    OUTDOORS,
    CREATIVE,
    SOCIAL,
    LEARNING,
    RELAXATION,
    CITY_EXPLORATION
}
